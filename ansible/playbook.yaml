---
- name: Main task
  hosts: all
  gather_facts: true
  roles:
    - role: upgrade_packages
      tags: packages
    - role: install_packages_common
      tags: packages
    - role: install_packages_development
      when: "ansible_facts['processor'] | select('search', 'Xeon') | list | length > 0"
      tags: packages

    - role: openssh_keypair
      tags: openssh
    - role: authorized_key
      when: "ansible_facts['processor'] | select('search', 'Xeon') | list | length == 0"
      tags: openssh

    - role: git_config
      tags: git

    - role: crypto_policy
      vars:
        crypto_policy_name: "DEFAULT"
      when: "ansible_facts['processor'] | select('search', 'Xeon') | list | length == 0"
      tags: crypto_policy
    - role: crypto_policy
      vars:
        crypto_policy_name: "LEGACY"
      when: "ansible_facts['processor'] | select('search', 'Xeon') | list | length > 0"
      tags: crypto_policy
